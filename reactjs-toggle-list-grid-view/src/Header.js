import React from "react";
import "./App.css";

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Truist Bank Logo" width="200"/>
        </header>
    );
};

export default Header;
